<div class="page-width">
  <div class="page-content">
    <header class="page-header">
      <h1 class="page-title">{{ page.title }}</h1>
    </header>
    <div class="rte">
      {{ page.content }}
    </div>
  </div>
</div>
